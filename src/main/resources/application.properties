# LSM KV engine (com.gladunalexander.lsmkv) configuration.

# Do not pre-parse PUT/PATCH/DELETE form bodies into parameters; the KV controller
# reads the raw request body itself so it stays agnostic to the client's Content-Type.
spring.mvc.formcontent.filter.enabled=false

spring.main.banner-mode=off
